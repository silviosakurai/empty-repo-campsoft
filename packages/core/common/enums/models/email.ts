export enum EmailType {
  DISPOSABLE = "disposable",
  GRATIS = "gratis",
}

export enum EmailBlock {
  YES = "Y",
  NO = "N",
}

export enum EmailBounced {
  NO_ERROR = "0",
  TEMPORARY_ERROR = "1",
  PERMANENT_ERROR = "2",
}

export enum EmailComplaint {
  NO = "0",
  YES = "1",
}
