export enum BannerStatus {
  ACTIVE = "ativo",
  INACTIVE = "inativo",
}

export enum BannerItemStatus {
  ACTIVE = "ativo",
  INACTIVE = "inativo",
}

export enum BannerItemFormat {
  HTML = "HTML",
  IMG = "IMG",
}
