export enum RoleContext {
  USER = "usuario",
  GROUP = "grupo",
}

export enum Role {
  SALES_SITE = 1,
  ADMIN_APP = 2,
  ADMIN_COMPANY = 3,
  SALES_MANAGER = 4,
  SALES_SUPERVISOR = 5,
  SELLER = 6,
}
