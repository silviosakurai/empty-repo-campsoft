{
  "name": "node-maniadeapp-backend",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "engines": {
    "node": "20.11.1"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "dev:partner": "turbo run dev --filter=partner_api",
    "build:partner": "turbo run build --filter=partner_api",
    "dev:public": "turbo run dev --filter=public_api",
    "build:public": "turbo run build --filter=public_api"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "turbo": "^1.12.4"
  }
}