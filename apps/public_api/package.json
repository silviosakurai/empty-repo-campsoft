{
  "name": "public",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "20.11.1"
  },
  "scripts": {
    "dev": "nodemon --watch 'src/**/*' -e ts,tsx --exec 'ts-node -r tsconfig-paths/register src/index.ts'",
    "build": "tsc && tsc-alias -p tsconfig.json",
    "lint": "pnpx eslint . --ext .ts,.tsx",
    "sonar": "sonar-scanner -X"
  }
}
