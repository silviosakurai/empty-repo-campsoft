import { FastifyReply, FastifyRequest } from 'fastify';

export const createCart = async (
  request: FastifyRequest,
  reply: FastifyReply
) => {
  try {
    console.log('');
  } catch (error) {
    console.log(error);
  }
};
